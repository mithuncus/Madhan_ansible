- name: ngnix playbook
  hosts: web
  tasks:
  - name: ngnix install n enable and start
    ansible.builtin.package:
    name: nginx
    state: present
  - name: Start service httpd, if not started
    ansible.builtin.service:
    name: nginx
    state: started